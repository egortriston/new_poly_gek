import {chromium,expect} from '@playwright/test';
const b=await chromium.launch({channel:'msedge',headless:true});try{const p=await b.newPage();await p.goto('http://127.0.0.1:5174/login');await p.getByRole('button',{name:'Заполнить данные для входа'}).click();await p.getByRole('button',{name:'Войти в систему',exact:true}).click();await p.goto('http://127.0.0.1:5174/oop/status');await p.getByRole('combobox',{name:'Направление подготовки',exact:true}).click();await p.getByRole('textbox',{name:'Поиск вариантов'}).fill('эконом');await expect(p.getByRole('option')).toHaveCount(1);await p.getByRole('option').click();await expect(p.locator('tbody tr')).toHaveCount(2);await p.getByRole('combobox',{name:'Направление подготовки',exact:true}).click();await expect(p.getByRole('option')).toHaveCount(6);await p.keyboard.press('Escape');await p.getByRole('button',{name:'Подразделы исходных данных ООП'}).click();await expect(p.getByRole('link',{name:'Исходные для направлений',exact:true})).toBeVisible();await p.getByRole('link',{name:'Исходные для ООП',exact:true}).click();await expect(p).toHaveURL(/oop\/data\/programs$/);console.log('PASS: type/filter/select, reset search, expandable navigation');}finally{await b.close();}
